# 贡献指南

## 基本原则

1. 所有开发记录、文档和面向维护者的信息使用简体中文。
2. 先确认当前阶段目标和验收条件，再修改代码；只实现当前可达需求。
3. 保持最小改动，不做无关清理、等价重构、预留式抽象或未经验证的性能承诺。
4. 原始资料只作证据来源。需求冲突由产品决策或架构决策解决，不在业务代码中猜测兼容全部口径。
5. 每个阶段完成后及时提交 Git；推送前必须检查变更内容，避免把依赖、构建产物、运行数据或敏感本地配置纳入版本库。

## 目录职责

- `docs/backgrounds/`：只读原始来源材料。
- `docs/sources/`：可追溯、可重复生成的历史来源提取结果。
- `docs/product/`：经筛选的产品事实、需求状态与数据契约。
- `docs/planning/`：范围、阶段、任务和验收标准。
- `docs/architecture/`：经确认的架构、接口和数据决策。
- `docs/guides/`：实现业务组件后建立，用于安装、运行、演示和故障定位说明。
- `src/`：前端、业务后端和算法实现，具体边界见 [`src/README.md`](src/README.md)。
- `scripts/`：仓库级检查，以及实现后确有用途的构建和运行辅助脚本。

不要从 `docs/backgrounds/` 或 `docs/sources/` 直接派生运行时配置或业务规则。需要进入实现的内容应先在规划、产品或架构文档中明确采用状态和验收方式。

## 工作流程

1. 阅读根目录 `AGENTS.md`、本文件及当前任务涉及目录的说明。
2. 检查工作区状态，保留他人的未提交修改。
3. 明确本次改动能被哪项检查或演示流程验证。
4. 实施最小完整改动并运行针对性检查。
5. 检查 `git diff --check` 和实际差异，再创建阶段性提交。
6. 仅在获得当前任务授权时推送；记录提交与验证结果。

仓库基础检查：

```powershell
py -3 scripts/validate_repository.py
git diff --check
```

业务组件建立后，应在各组件说明中补充精确的构建和测试命令；根目录只保留能够实际执行的统一入口。

## 提交要求

- 一个提交对应一个清晰目标，提交信息说明产生的结果。
- 不提交 `node_modules`、Python 虚拟环境、Java 构建目录、前端构建目录、IDE 状态、日志或本地数据库数据。
- 不提交真实账号、口令或未经脱敏的生产数据；示例数据应明确标注为合成数据。
- 不用空文件、空接口或固定成功返回值伪装完成状态。

## 完成标准

“完成”至少意味着：范围内实现存在、针对性检查通过、运行或验收步骤可复现、限制和未纳入范围已如实记录。仅有设计文档或占位代码不能宣称功能完成。

# M8 正式产品基线与来源闭环证据

- 阶段/门槛：M8 / G8
- 固定候选：`b1f3c160ab3f26d9d82948d8e66b4600c768af7c`
- 基线：`21e69f097f88fa75fa8442694dafbdd09ffcd402`（`v0.1.0`、`main`/`dev` 同步点）
- 候选分支：`origin/feat/m8-formal-baseline`
- 复核日期：2026-08-26
- 本机平台：Windows 11 x64、WSL2、Docker Desktop
- 独立复审：PASS
- 最终判定：G8 通过，已形成 `dev` 远端检查点

## 实现范围

M8 把正式运行路径、根说明、报告、Windows 发布包和 CI artifact 统一为“报警管理系统”与 `alert-management-system`，并保留“合成示例数据”“演示数据复位”等真实功能边界。原始 PDF/DOCX、逐字节提取结果和 M0–M7 历史证据未改写。

来源闭环矩阵覆盖 `SC-001..SC-057`，每项唯一归入已实现、M9–M14 的具体阶段、明确拒绝或带重启条件的外部关闭项。M8 同时冻结 hybrid-v2 模型卡、安全威胁模型、M8–M14 工作流和只允许已验证 `main` 提交打功能标签的版本策略。本阶段没有修改算法运行代码、数据库迁移、新业务 API 或 M9–M14 功能。

## 固定候选本机验收

固定候选按 `automation/workflow.json` 的顺序执行全部 G8 命令：

- `python3 scripts/validate_repository.py`：PASS。
- `python3 scripts/validate_automation.py`：PASS。
- `python3 scripts/validate_formal_baseline.py`：PASS；正式产品身份、57 项来源能力和 M8–M14 阶段闭环。
- `npm --prefix tools/document-extraction run check`：PASS；37 个文件逐字节一致。
- `scripts/dev/test.sh`：PASS；Java 34、Python 算法 28、合成数据 5、跨组件契约 4、Vue 12 项测试全部成功。
- `scripts/dev/restart-smoke.sh`：PASS；两轮停止、启动、聚合健康、Vue 静态页面身份和 Flyway 迁移幂等检查成功。

验收结束后执行 `scripts/dev/stop.sh`，应用进程和开发数据库容器已停止，数据库卷保留。

## 远端固定提交检查

全部结果的 `headSha` 均为固定候选：

- 仓库基础检查：run `32887741185`，SUCCESS；实际执行正式产品基线校验。
- M1 应用骨架检查：run `32887741328`，SUCCESS。
- M6 Windows 原生发布检查：run `32887741264`，SUCCESS；标准用户双目录正式验收完成。
- Windows artifact：`alert-management-system-windows-x64`，构建和保存成功。

M8 最终修复只修改 Windows 构件名、正式文档和校验覆盖，因此 Docker 工作流未被路径过滤器再次触发；包含运行身份变更的父候选 `dbb2f4576051a74426be23c6b06772345604c6fe` 已通过 Docker run `32886319250`。G8 本身不把 Docker G7 作为重复验收命令。

## PR 与 dev 集成检查点

PR #2 `feat/m8-formal-baseline → dev` 在无评论、无审查冲突、合并状态 CLEAN 且四条 PR 发布级检查全部成功后，以 merge commit 合入：

- PR：`https://github.com/Selloryebir/alert_management_system/pull/2`
- 合并提交：`d93e98898991eb34a294711e88d631c888422879`
- 两个父提交：原 `dev` `21e69f097f88fa75fa8442694dafbdd09ffcd402` 与候选证据提交 `e5e705b46366e952a91f12caafc91361fae7da9c`
- 合并树：`aab1f30fdc7778bfe0b0102840bc692911fca4b3`，与受审分支树完全一致

PR 检查均成功：仓库 run `32889075875`、应用 run `32889075860`、Windows 原生 run `32889075878`、Docker G7 run `32889075921`。

`dev` 合并提交随后独立重跑四条流水线，`headSha` 均精确匹配 `d93e98898991eb34a294711e88d631c888422879`：

- 仓库基础检查：run `32889911296`，SUCCESS。
- M1 应用全链：run `32889911320`，SUCCESS。
- Windows 原生发布检查：run `32889911299`，SUCCESS。
- Docker Compose G7：run `32889911250`，SUCCESS。

因此该 merge commit 是 M8 可回退、已远端核验的阶段检查点；状态迁移提交只记录本事实并解锁 M9，不改变受审实现树。

## 独立审查

未参与主实现的审查智能体先对第二候选 `968e5c43f0bbc8bf2813100ef1dc86c14c010a3c` 给出 FAIL：Windows 目录、ZIP、manifest 和 artifact 仍含旧构件名，校验器漏扫真实构建路径，标签策略也在 `dev`/`main` 间冲突。该候选未作为通过证据。

第三候选逐项修复后，独立复审结论为 PASS：

- 本地 `HEAD`、远端跟踪分支和 `git ls-remote` 精确一致，工作树干净。
- Windows 四处用户可见构件统一新名；内部 Docker label 作为稳定技术键保留，避免破坏既有开发容器备份。
- 正式基线校验器扫描真实构件路径并接入 PR CI；临时把当前阶段设为 M8–M14 时七阶段均可通过，不会锁死后续流程。
- 57 项来源能力连续、唯一；抽查已实现项没有提前宣称 M9–M14 能力。
- PRD-022/SC-046 明确区分“拒绝历史固定阈值”和“外部输入齐全后新建性能基准”；无责任延期已从冲突登记中移除。
- hybrid-v2 模型卡明确 v1 的固定分数、关键词和场景步骤耦合，规定显式时序公式、Markov episode 统计、可弃权原因模型和泛化/负控验收，不把分数冒充概率或真实准确率。
- 来源、历史证据、样例、算法运行代码和数据库迁移相对基线零差异。

## 缺陷闭环与边界

M8 共固定三次候选。第一次补齐身份后发现正式校验器未接入远端 CI；第二次接入 CI 后由独立审查发现 Windows 构件名和标签边界仍不一致；第三次只修复这些可复现问题并通过全部门槛。

首次把组件测试与来源提取并行调度时，组件测试以 141 退出且没有组件失败输出；按验收定义改为顺序执行后稳定通过。首次重启冒烟在应用启动前中止，容器退出码为 0 且无新应用日志；随后带跟踪完整启动及两次正式重启验收均通过，没有证据支持修改业务代码。两项均保留在状态文件中，不作为成功结果。

Windows 测试工具的嵌入式 PostgreSQL `initdb` 子进程输出受宿主代码页影响显示问号；发布服务日志未复现，业务断言全部成功。该问题已明确交给 M12 中文编码和日志可靠性审计，不外推为当前用户界面乱码。

本证据证明正式产品身份、来源责任、后续阶段、研究边界和自动化门槛已经形成一致且可执行的基线。它不证明 M9 算法、M10 项目化、M11 网络安全、M12 恢复可靠性、M13 正式文档或 M14 终验已经完成。

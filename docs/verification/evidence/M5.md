# M5 报告、审计与演示复位复核证据

- 阶段/门槛：M5 / G5
- 固定候选：`584f7a4cfa6a3c5a99370083008dd5d73f215f38`
- 候选分支：`origin/feat/m5-report-audit-reset`
- `dev` 非快进合并检查点：`de98a05eed9052383a30881ef85760440cf93667`
- 复核日期：2026-08-25
- 本机平台：Windows 11 x64、WSL2、Windows JDK 21.0.8、Python 3.12.14、Node.js 22.22.1、Docker Desktop、PostgreSQL 17.6、Chromium
- 独立审查：PASS
- 最终判定：G5 通过，可进入 M6

## 实现范围

候选在既有 Java、Vue、Python 和 PostgreSQL 边界内实现整次已完成分析的 PDF/XLSX 导出、统一业务审计、最小人工分类修订、项目数据库备份和限定业务表的演示复位。人工修订保留算法原值并单独记录有效分类、操作者、理由和时间；报告和页面读取同一 PostgreSQL 事实。

PDF 使用随包开源中文字体；输出边界把控制字符规范为空格，把字体确实不能编码的字符替换为 `?`，避免合法导入字段造成报告失败。XLSX 包含摘要、报警明细、事件链和处置历史四个工作表。复位拒绝运行中的分析，只清理本项目业务表，不删除迁移、非业务表、备份或项目外资源。

本阶段没有建设报告任务表、异步队列、文件仓库、权限系统、通用工作流或第二套业务实现。

## 作者检查

执行：

```text
scripts/dev/test.sh
scripts/dev/m5-report-audit-reset-smoke.sh
git diff --check fcdf0ef652a99c285f616fc6e59aa4d869083d05..584f7a4cfa6a3c5a99370083008dd5d73f215f38
```

结果：后端 34 项、算法 28 项、合成数据 5 项、跨组件契约 4 项、前端 12 项，共 83 项通过。真实 Chromium 完成：

- 两轮 300 行报告、审计、分类修订、处置和复位闭环，规范化摘要一致，耗时 13,899 ms。
- 20,000 行页面分析与两类报告，整轮耗时 18,332 ms；PDF 1,152 ms / 21,340 bytes，XLSX 1,843 ms / 2,346,969 bytes。
- PostgreSQL 自定义格式备份可读；复位后业务表为空，非业务 SQL 哨兵保留；成功退出后无残留 PID、端口或项目容器。
- PDF 边界集成回归确认中文保真、换行转空格、emoji 缺字转问号，并可由 PDFBox 打开和提取。

前三个固定候选分别暴露跨环境 Blob 测试夹具、脚本可执行位和 PDF 内嵌换行问题。每项均按真实复现修复并增加对应门槛，没有跳过测试或降低验收要求；第四候选才进入最终通过结论。

## 独立审查记录

未参与 M5 实现的审查智能体只读复核固定候选，结论为 PASS：

- HEAD 与远端候选一致，工作树干净；41 个变更路径全部位于活动运行白名单，`git diff --check`、状态文件和脚本模式通过。
- `scripts/dev/test.sh` 83/83 通过。
- M5 真实 Chromium 冒烟的 300 行双轮耗时 12,327 ms；20,000 行耗时 17,377 ms；PDF 900 ms / 21,437 bytes，XLSX 1,881 ms / 2,347,185 bytes。
- 独立构造 `area="北区\n二线🚀"` 的合法 CSV，状态依次为 `READY -> IMPORTED -> COMPLETED`；PDF 返回 200、20,793 bytes，PDFBox 可打开一页，提取结果包含 `北区 二线?=1` 且不保留原始换行。
- XLSX 四个工作表可解包，300 条明细、人工修订前后分类和两条处置历史均与数据库一致；导出审计九项字段完整。
- 备份为可读 PGDMP；复位范围、事务语义和 SQL 哨兵通过；结束后无错误日志、PID、8080 服务或运行中的项目容器。

## 远端与集成检查点

固定候选远端执行：

- 仓库基础检查：成功，run `32852755809`。
- 应用检查：成功，run `32852755714`；组件测试和 M1 至 M5 全链路均通过。M5 的 300 行双轮耗时 5,841 ms，20,000 行耗时 9,915 ms；PDF 863 ms / 21,394 bytes，XLSX 1,642 ms / 2,347,319 bytes。

`dev` 非快进合并提交 `de98a05eed9052383a30881ef85760440cf93667` 已推送且由远端重新执行：

- 仓库基础检查：成功，run `32853607073`。
- 应用检查：成功，run `32853607128`；后端 34、算法 28、数据 5、契约 4 及前端测试通过，M1 至 M5 全链路均成功。M5 的 300 行双轮耗时 7,532 ms，20,000 行耗时 12,999 ms；PDF 1,164 ms / 21,306 bytes，XLSX 2,174 ms / 2,346,844 bytes。

GitHub Actions 仍提示部分官方 action 的 Node 兼容层及 `setup-java@v4` 弃用；该提示不改变退出码或本阶段结果，不在 M5 做无关升级。

## 证据边界

本记录证明固定合成数据上的报告、审计、人工分类修订、项目备份和演示复位闭环，也证明包含控制字符及字体缺字的合法字段不会使 PDF 导出失败。它不证明真实工业准确率、权限与合规能力，也不证明 M6 Windows 自包含发布包或 M7 Docker 次级交付已经完成。

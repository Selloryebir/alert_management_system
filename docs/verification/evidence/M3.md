# M3 算法与分析闭环复核证据

- 阶段/门槛：M3 / G3
- 最终固定候选：`0435408e1fd5fcabf583a951810c58cee0bf343a`
- 候选分支：`origin/feat/m3-analysis-pipeline`
- `dev` 非快进合并检查点：`dacfdf28d9b79a7f9ccd6b24dae339846f65e6fe`
- 复核日期：2026-08-25
- 本机平台：Windows 11 x64、WSL2、Windows JDK 21.0.8、Python 3.12.14、Node.js 22.22.1、Docker Desktop、PostgreSQL 17.6
- 独立审查：第二次固定候选 PASS
- 最终判定：G3 通过，可进入 M4

## 实现范围

候选实现 Python `POST /api/v1/analyze` 纯计算接口、严格 v1 请求/响应模型、`rules-v1.0.0` 规则、重复/抖动/短时/持续分类、原因类别建议和关联事件链。事件链同时受时间窗及 `site/area/unit` 关系键约束，说明明确其仅为关联建议，不代表已确认根因。

Java 实现同步分析编排、超时与失败重试、响应契约校验、Flyway V3 分析表和 PostgreSQL 原子保存。Java 会拒绝版本、运行 ID、汇总、成员、时间窗或关系范围不合法的算法响应；失败运行保留可重试原因，但不保存部分结果或事件链。

独立黄金文件基于固定 `source_row` 场景声明和输入时间构建，不导入算法实现或读取算法输出。当前 Smoke SHA256 为 `329e260e7330bd5897600bae41ca61bc2f29aca137f9b6fdffa29c4c40199e68`。

## 作者检查

执行：

```text
scripts/dev/test.sh
scripts/dev/m3-analysis-smoke.sh
git diff --check 3959ebe6495db604b6bb981ce38778e59b944075..0435408e1fd5fcabf583a951810c58cee0bf343a
```

结果：后端 29 项、算法 28 项、合成数据 5 项、跨组件契约 4 项、前端 5 项通过。实际 Java、Python 与 PostgreSQL 17.6 闭环结果：

- 同一 300 行 Smoke 导入两次，逐记录分类、分值、证据、汇总和 12 条事件链语义一致。
- 噪声类型精确为 `NORMAL=170`、`DUPLICATE=30`、`CHATTER=40`、`SHORT_LIVED=30`、`PERSISTENT=30`。
- 原因类别精确为 `PROCESS_DISTURBANCE=90`、`EQUIPMENT_FAULT=30`、`INSTRUMENT_ISSUE=30`、`MAINTENANCE_TEST=20`、`UNKNOWN=130`。
- 12 条链均为五个有序成员、位于 60 秒窗口内且共享同一关系键。
- 停止算法服务后分析明确为 `FAILED`，结果和事件链均为零；恢复后同批次 `attempt=2` 成功且无重复写入。
- 固定种子 20,000 行完成分析，本轮作者检查耗时 4,547 ms。

嵌入式 PostgreSQL 集成测试另覆盖 HTTP 503、超时、非法 JSON、非法版本/汇总、超窗链、跨关系链、强制完成失败整体回滚、重复启动和不存在资源。

## 独立审查记录

第一次固定候选 `8f672ad1b476de3db7a7da9667e8531afd05c65b` 的独立审查为 FAIL：算法会将不同 `site/area/unit` 的五个步骤错误拼接为链，Java 也会接受超过请求时间窗的链；审查还发现活动运行白名单漏列实际修改的 `automation/workflow.json`。没有降低需求或绕过门槛。

修复后，算法按类型和完整关系键分组；合成链场景及黄金前置条件显式保证关系一致；Java 同时校验时间窗和关系范围，并新增真实数据库零写入断言。第二次固定候选 `0435408e1fd5fcabf583a951810c58cee0bf343a` 的独立审查为 PASS：

- `scripts/dev/test.sh`：后端 29、算法 28、数据 5、契约 4、前端 5 项全部通过。
- `scripts/dev/m3-analysis-smoke.sh`：300 行两次一致、12 条合法链、故障零写入与第二次尝试恢复均通过；20,000 行耗时 4,524 ms。
- 直接构造不同单元的五步输入得到零条链；Java 超窗和跨关系响应测试均得到 `FAILED` 且数据库结果/链为零。
- HEAD 与远端 SHA 一致、工作树干净、基线差异无空白错误，39 个修改路径全部位于活动运行白名单。

独立复核首次执行 `test.sh` 时出现一次 Vitest worker 启动超时；前端单独复跑及随后整条命令均通过，未复现且不影响业务结果，记录为非阻断运行时抖动。

## 远端与集成检查点

最终候选远端执行：

- 仓库基础检查：成功，run `32840712784`。
- 应用检查：成功，run `32840712669`；执行全部组件测试、两轮重启、M2 导入及 M3 分析冒烟，20,000 行分析耗时 3,134 ms。

`dev` 非快进合并提交 `dacfdf28d9b79a7f9ccd6b24dae339846f65e6fe` 已推送且由远端重新执行：

- 仓库基础检查：成功，run `32841454187`。
- 应用检查：成功，run `32841454102`；完整 M3 闭环通过，20,000 行分析耗时 2,840 ms。

GitHub Actions 仍提示部分官方 action 的 Node 20 兼容层及 `setup-java@v4` 弃用；该提示不改变退出码或本阶段结果，不在 M3 做无关升级。

## 证据边界

本记录证明固定合成数据上的可解释规则、原因建议、关联事件链、Java—Python 契约、失败零写入和恢复重试。它不证明真实工业数据准确率，也不证明 M4 浏览器看板/处置、M5 报告审计、M6 Windows 自包含发布包或 M7 Docker 交付已经完成。

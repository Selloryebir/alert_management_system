# M4 前端业务闭环复核证据

- 阶段/门槛：M4 / G4
- 固定候选：`afe6b57904175d265b6c4af6001e9a25f15211dc`
- 候选分支：`origin/feat/m4-business-workflow`
- `dev` 非快进合并检查点：`937320e8df2e7959f368f4c4074621dd66a922f2`
- 复核日期：2026-08-25
- 本机平台：Windows 11 x64、WSL2、Windows JDK 21.0.8、Python 3.12.14、Node.js 22.22.1、Docker Desktop、PostgreSQL 17.6、Chromium
- 独立审查：PASS
- 最终判定：G4 通过，可进入 M5

## 实现范围

候选在既有 Java 单体和 Vue 单页边界内实现分析运行定位、PostgreSQL 聚合看板、分页筛选、报警详情、原始行与规则证据追溯、关联事件链，以及人工处置当前态和历史。事件链在页面明确标注为关联建议，不构成因果结论。

处置状态固定为 `OPEN`、`IN_PROGRESS`、`CLOSED`，合法流转为 `OPEN -> IN_PROGRESS`、`IN_PROGRESS -> OPEN|CLOSED`、`CLOSED -> IN_PROGRESS`。操作者和说明均必填；当前态与历史在同一数据库事务内更新并使用行锁，读取按分析运行隔离。统计、分析结果和处置事实均来自 PostgreSQL，前端不复制计算或业务状态机。

## 作者检查

执行：

```text
scripts/dev/test.sh
scripts/dev/m4-browser-smoke.sh
git diff --check f3660b00867e5f4a466dd73c99ddf2dc320b654e..afe6b57904175d265b6c4af6001e9a25f15211dc
```

结果：后端 31 项、算法 28 项、合成数据 5 项、跨组件契约 4 项、前端 8 项通过。Flyway 在真实 PostgreSQL 上成功应用 V1 至 V4。Chromium 实际完成：

- 300 行文件上传、预览确认、分析、看板精确统计、空组合筛选、详情、原始行、证据、12 条事件链、`OPEN -> IN_PROGRESS -> CLOSED` 处置及历史；本轮耗时 9,715 ms。
- 浏览器拦截算法调用模拟不可用，页面给出明确可重试中文提示，未显示假成功。
- 固定种子 20,000 行由页面上传、确认、分析并打开看板首屏；本轮耗时 14,461 ms。

首次直接使用 Chromium 时发现 WSL 缺少运行库，脚本调整为仅在忽略的 `.runtime/` 解压所需 Debian 包，不修改系统软件。DrvFS 上 Vitest `threads` worker 曾启动超时；改用单 worker `forks` 后单独前端测试和全量测试均稳定通过。两项失败均保留在机器状态记录中，没有跳过测试或降低验收要求。

## 独立审查记录

未参与 M4 实现的新审查智能体仅对固定候选 `afe6b57904175d265b6c4af6001e9a25f15211dc` 进行只读复核，结论为 PASS：

- HEAD 与远端候选 SHA 一致，工作树干净；基线差异共 30 个路径，全部位于活动运行白名单，且无空白错误、占位或验收绕过。
- `scripts/dev/test.sh` 共 76 项测试通过，无失败或跳过。
- `scripts/dev/m4-browser-smoke.sh` 的 300 行真实 Chromium 主流程 2/2 通过，耗时 9,804 ms；20,000 行页面主流程 1/1 通过，耗时 15,663 ms。大数据模式只按设计跳过故障注入用例，没有跳过业务主流程。
- 审查确认看板来自 PostgreSQL，页面数字与固定 API 一致；处置事务、行锁、状态转换及运行隔离成立；事件链没有因果断言；日志没有未处理异常。

## 远端与集成检查点

固定候选远端执行：

- 仓库基础检查：成功，run `32844604157`。
- 应用检查：成功，run `32844604388`；执行组件测试、两轮重启、M2 导入、M3 分析及 M4 浏览器闭环。

`dev` 非快进合并提交 `937320e8df2e7959f368f4c4074621dd66a922f2` 已推送且由远端重新执行：

- 仓库基础检查：成功，run `32845315864`。
- 应用检查：成功，run `32845315809`；后端 31、算法 28、数据 5、契约 4 项通过，M4 的 300 行浏览器闭环耗时 4,278 ms，20,000 行页面流程耗时 9,640 ms。

GitHub Actions 仍提示部分官方 action 的 Node 兼容层及 `setup-java@v4` 弃用；该提示不改变退出码或本阶段结果，不在 M4 做无关升级。

## 证据边界

本记录证明固定合成数据上的浏览器导入、分析、看板、详情、关联事件链、人工处置和可行动失败提示。处置历史已经可查，但统一业务审计、人工分类修订、PDF/XLSX 报告和演示复位属于 M5；本记录也不证明 M6 Windows 自包含发布包或 M7 Docker 交付已经完成。

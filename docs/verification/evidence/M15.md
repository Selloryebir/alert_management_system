# M15 产品工作台与工业化视觉验收证据

## 候选检查点

- 集成分支：`dev`
- 实现提交：`3b8706fa7d65ba4c578345949bd5c27abcf825bf`
- 检查点提交：`b5d22118079a7693c8fababef8a6629f59995abd`
- 验收日期：2026-08-30（Asia/Shanghai）
- 远端状态：检查点已推送到 `origin/dev`，适用流水线全部成功。

## 实现结果

- Dashboard、数据导入、分析任务、报警处置、报告审计、项目管理、权限管理、备份恢复、运行状态已拆分为独立页签。
- 当前项目与分析上下文在页签之间复用；权限和备份页签只向有权用户呈现。
- 首页、业务空状态、关联链路、报告及数据重置文案已改为正式产品叙事；必要分析边界只在新建系统管理员账号时显示。
- 默认初始项目在展示层使用正式名称，不修改后端兼容标识和接口契约。
- 小时趋势移除逐点圆形散点，改用 2.25 像素连续折线和辅助网格；新增 20,000 个聚合时段坐标的有界性测试。
- 视觉基线改为深色顶栏、紧凑页签、低饱和状态色、硬边界和网格背景组成的工业工作台层级。

## 验收命令与结果

以下阶段命令在实现提交 `3b8706fa7d65ba4c578345949bd5c27abcf825bf` 及最终检查点上成功执行：

1. `npm --prefix src/frontend run lint`：通过。
2. `npm --prefix src/frontend test -- --run`：通过，5 个测试文件、40 项测试全部成功。
3. `npm --prefix src/frontend run build`：通过，Vue/TypeScript 类型检查与 Vite 生产构建成功。
4. `python scripts/validate_automation.py`：通过。
5. `python scripts/validate_repository.py`：通过，作为额外仓库基础检查。

验证所需 Node.js 22.17.1 仅供应到被忽略的 `.runtime/toolchains`，未纳入源码提交。

## 发布回归

- `python scripts/validate_repository.py`、`python scripts/validate_formal_baseline.py` 与 `python scripts/validate_release_candidate.py --mode released`：通过。
- Windows 原生业务终验在 ASCII 目录和中文空格目录各执行一轮；启动、首次登录改密、项目创建、导入分析、报告审计、备份、隔离恢复、保留策略、数据重置和精确清理全部通过。验收根目录为 `.runtime/native/dc2-final-ui-retest/20260830-000346-99e8aa3c`。
- 本机回归所用 ZIP 源提交为 `dc2fa15c88cc48b75a73cffb860b48d169312c45`，SHA-256 为 `90233c8ad684cf11f283892af66a911cc5ad4552f20507e44ff2180656dedf80`；检查点 `b5d2211` 相比该源码树仅增加两处 E2E 结果断言，并由下列精确提交远端流程复验。

## 远端检查点

- 仓库基础检查：run `33262298455`，`SUCCESS`。
- M1 应用骨架检查：run `33262298460`，`SUCCESS`。
- Windows 原生业务发布检查：run `33262298493`，`SUCCESS`；以标准用户完成打包与业务预检查。
- Docker Compose 与 M12 可靠性工作流未被仅涉及 E2E 断言的末端提交重新触发；同一增量最近适用提交 `d9e97174eb14ac0924073784cfe2639585039979` 的 run `33258824985` 与 `33258824983` 均为 `SUCCESS`。

## 问题闭环

- 旧冒烟和浏览器断言仍查找已删除的“演示/示例”叙事或假设所有功能同页可见；已统一改为正式产品文案并通过页签导航定位。
- 报告标题、数据重置确认词、结果文案和隐藏页签空状态与新界面不一致；断言已逐项对齐可见业务结果。
- Windows 本机复验曾在 23:59 与计划备份任务恰好碰撞；清理精确实例后跨日重跑，两轮完整通过，未将该环境时序碰撞记为产品成功证据。

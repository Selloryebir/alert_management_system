# M15 产品工作台与工业化视觉本地验收证据

## 候选检查点

- 分支：`feat/m15-product-workspace`
- 实现提交：`3b8706fa7d65ba4c578345949bd5c27abcf825bf`
- 验收日期：2026-08-28（Asia/Shanghai）
- 远端状态：未推送，按项目负责人要求不为阶段内小改动触发 CI。

## 实现结果

- Dashboard、数据导入、分析任务、报警处置、报告审计、项目管理、权限管理、备份恢复、运行状态已拆分为独立页签。
- 当前项目与分析上下文在页签之间复用；权限和备份页签只向有权用户呈现。
- 首页、业务空状态、关联链路、报告及数据重置文案已改为正式产品叙事；必要分析边界只在新建系统管理员账号时显示。
- 默认初始项目在展示层使用正式名称，不修改后端兼容标识和接口契约。
- 小时趋势移除逐点圆形散点，改用 2.25 像素连续折线和辅助网格；新增 20,000 个聚合时段坐标的有界性测试。
- 视觉基线改为深色顶栏、紧凑页签、低饱和状态色、硬边界和网格背景组成的工业工作台层级。

## 验收命令与结果

以下命令均在实现提交 `3b8706fa7d65ba4c578345949bd5c27abcf825bf` 上成功执行：

1. `npm --prefix src/frontend run lint`：通过。
2. `npm --prefix src/frontend test -- --run`：通过，5 个测试文件、40 项测试全部成功。
3. `npm --prefix src/frontend run build`：通过，Vue/TypeScript 类型检查与 Vite 生产构建成功。
4. `python scripts/validate_automation.py`：通过。
5. `python scripts/validate_repository.py`：通过，作为额外仓库基础检查。

验证所需 Node.js 22.17.1 仅供应到被忽略的 `.runtime/toolchains`，未纳入源码提交。

## 尚未执行

- 未推送分支，未触发远端 CI。
- 未生成 Windows 正式发布包；本阶段没有修改后端、数据库、原生脚本或发布契约。
- 阶段保持 `review`，待项目负责人查看实际浏览器视觉后，再决定是否推送并进入合入流程。

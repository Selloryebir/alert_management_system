# M14 Windows 11 业务用户终验与发布候选证据

- 阶段/门槛：M14 / G14
- 固定实现候选：`53b8ff3e0d6768543bd7a3c8cc0295f5dc3e7b98`
- 候选分支：`feat/m14-process-stop-fix`
- 自动复核日期：2026-08-27
- 目标平台：Windows 11 x64；Docker Compose 为次级部署路径
- 发布目标：`v1.0.0`
- 自动门槛判定：PASS
- 项目负责人验收声明：PASS；已授权完成文档后执行 `v1.0.0` 正式发布

## 固定候选与正式产物

Windows 自包含候选：

```text
E:\code\alert_management_system\.runtime\release\artifacts\53b8ff3e0d6768543bd7a3c8cc0295f5dc3e7b98\alert-management-system-windows-x64-53b8ff3e0d67.zip
```

SHA-256：

```text
ea09168a5ea318dd0680c58ec5867ed8b7693f2fcb884a87b445b71a259fc9c0
```

受审源码包由固定候选的 320 个公开跟踪文件构成；独立生成两次的 SHA-256 均为：

```text
b54a82d9405e56fb2dcf834e558d24d4577b124190fa7f558d7d4987dc1aa2d7
```

源码包排除 `.git`、`.runtime`、`AGENTS.md`、`automation`、内部规划/审计/验证目录及历史来源提取链，且通过 UTF-8、公开叙事词和相对链接完整性检查。固定候选当时的九份正式 Markdown 与 DOCX/PDF 确定一致；正式发布补充办公室人员说明和专利主体草案后扩展为十份，重新生成并由静态门槛验证。

## 工程质量与完整测试

以下固定入口均在候选提交上成功执行：

```bash
scripts/dev/quality.sh
scripts/dev/test.sh
.runtime/deliverables-venv/bin/python tools/deliverables/build.py --check
python3 scripts/release/export_clean_source.py --check
python3 scripts/validate_formal_baseline.py
python3 scripts/validate_release_candidate.py
```

质量结果：Java 编译与 Checkstyle、Python Ruff、前端 ESLint 通过；Maven 模型和编译未产生未处置告警。测试结果为 Java `51`、算法 `50`、合成数据 `8`、跨组件契约 `4`、前端 `39`，均为零失败。正式基线核对 57 项需求能力均已有实现、明确边界或正式否决结论，无悬空项。

开发原地启动还从缺少模型文件的状态完成两轮启停，算法服务、数据库和主程序健康均为 UP；每轮在明确的开发运行目录原子重建模型，停止后不遗留应用进程。

## 数据、算法与安全边界

工程场景数据为 48 行，训练/验证/测试按场景组隔离为 `24/12/12`，并拒绝跨集合的组、文本和完整结构特征重复。监督分支采用字符 TF-IDF + LinearSVC 与结构特征 AdaBoost，结合既有可解释规则与保守拒识；模型以认证加密形式保存，密钥使用独立文件，残缺产物不会被当作有效模型。

固定工程场景验证集和测试集各 12 条：最终模型各接受 5 条，5 条均与工程标签一致，错误接受为 0，其余 7 条返回 `UNKNOWN` 交由规则或人工处理。该结果仅证明项目自建场景上的契约与拒识行为，不代表现场准确率、召回率或法规认证。中文正常运行描述、英文 `abnormal` 边界、故障后恢复描述及中断写入均有回归测试。

上传大小、扩展名、结构、字段映射和枚举错误均在提交前阻断；数据库写入使用参数化访问，登录会话和权限边界受测。网络模式使用 HTTPS；本机原生模式仅绑定回环地址。未加入与当前部署威胁模型无关的复杂安全设施。

## Windows 11 原生发布验收

固定命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/release/verify-business-release.ps1
```

结果：标准用户 `LAPTOP_BI1UHW\hears` 执行两轮全新目录验收 PASS。范围包括损坏 ZIP 拒绝、固定端口占用提示、ASCII 与中文空格目录、首次登录改密、项目和账号、CSV/TXT/XLSX、多类有效与无效样例、数据库驱动 Dashboard、分析解释、证据链、人工修订与处置、PDF/XLSX 报告、20,000 行数据、备份状态、跨实例隔离恢复、重复启停、计划任务/PID/端口清理。摘要：

```text
E:\code\alert_management_system\.runtime\release\verification\20260827-163656-648700f6\verification-summary.json
```

发布总摘要为 `status=PASS`、`standard_user_precheck=PASS`、`corrupt_archive_rejected=true`、`human_business_acceptance=REQUIRED`，并精确绑定候选提交与上述 ZIP 哈希。

本地视觉清单位于以下忽略目录，共 28 张 PNG，不进入 Git：

```text
.runtime/release/verification/20260827-163656-648700f6/round-1-results/visual-inventory/53b8ff3e0d67/
```

桌面 `1440×1000` 与窄屏 `390×844` 各覆盖 14 个主要页面/状态。人工复核确认错误文件弹窗、趋势图、占比图、统计卡、数据库表格和详情证据链可见，未见遮挡、图标漂移、数轴错位或中文乱码；统计值来自本轮导入与数据库查询，不是静态演示映射。

## Docker Compose 干净部署验收

固定命令：

```bash
python3 tests/smoke/run.py --target docker --fresh-volume
```

结果：PASS，耗时 `150.199` 秒。空数据卷和空模型卷完成本机与 HTTPS 启动，主动注入的残缺模型被精确重建；健康失败注入、三格式导入、固定分析、处置审计、本机/HTTPS 摘要一致性和资源清理均通过。摘要：

```text
.runtime/m7/20260827-164615-133926-c5d8fc09/verification-summary.json
```

摘要关键字段为 `fresh_volume=true`、`health_failure_injection=PASS`、`partial_model_recovery=PASS`、`cleanup=PASS`、`source_dirty=false`。

## 远端固定提交检查

下列 GitHub Actions 运行的 `headSha` 均精确为固定候选：

- 仓库基础检查：run `33055817337` 与 `33055955761`，SUCCESS。
- M1 应用骨架检查：run `33055817214` 与 `33055955844`，SUCCESS；两次均完整跨过 M4→M5 关停边界。
- M12 恢复质量与可靠性：run `33055817229` 与 `33055955802`，SUCCESS。

Windows 原生和 Docker 在固定候选上由上述本机正式命令重新完整执行。其上一产品提交 `aefe9bd` 的远端 Docker run `33048600677` 与 Windows run `33048599592` 也为 SUCCESS；最终候选相对它只修改开发进程关停函数，不修改原生包、Compose、产品代码、模型、样例或交付手册。固定候选的本机全量结果仍作为最终产物证据，不以旧提交的远端结果替代。

证据治理提交 `a3f8f18` 未改变固定候选产品树。其 M1 run `33056935011` 完整 SUCCESS；同一 headSha 的并行 run `33057041764` 在 M3 重新供应隔离运行时、尚未进入前端或 Java 构建前退出，未输出产品异常栈，清理步骤 SUCCESS。该失败不作为成功证据，也不覆盖同一 headSha 的完整成功结果；最终 PR 头仍须重新通过 M1 才可合并。

## 对抗审计与缺陷闭环

部署、安全、算法、UI 和文档三路独立审计首先提出以下真实缺陷，随后均按最小范围修正并由上述门禁覆盖：

- Compose 手工准备错误依赖被忽略的开发虚拟环境，且残缺模型会阻断后续启动；现已分离密钥职责并支持模型原子生成/精确恢复。
- Windows 英文区域控制台不能输出训练状态；现强制 UTF-8，远端 Windows 已经过该路径。
- 常见中文正常运行描述和英文 `abnormal` 子串会造成低级误判；现加入 Unicode 归一化、词边界和高置信否定/正常语义保护。
- 原验证数据与训练结构重复；现按组、文本和结构签名隔离，并公开保守接受率与适用边界。
- Dashboard 缺少可见刷新和数据时间，导入网络错误缺少警报语义；现提供数据库快照时间、刷新入口与 `role=alert`。
- 公开源码曾保留内部目录断链和早期编写叙事；现由净化导出器阻断禁止路径、词语和相对断链。
- 开发启动从空目录缺少模型；现仅在开发入口原子重建，并由本地两轮启停及远端 M1/M12 验证。
- 合并后 `dev` 的 M1 首次运行在 M4 完成后遇到 Java 进程 10 秒关停窗口，旧 PID 文件阻断 M5；同一提交复跑虽通过，仍按可达时序缺陷处理。现识别僵尸进程，优雅关停超时后仅对命令行仍匹配本项目的 PID 执行有限强制关停，并由定向忽略 TERM 测试和固定候选两次远端 M1 完整验证。

## 项目负责人验收声明

项目负责人在当前会话中已经完成实际审查，并给出以下原始声明：

```text
人工已验证不存在大问题，符合交付预期。
```

同一请求明确要求在完成最后一轮文档与静态检查后进行 `v1.0.0` 正式发布。为避免虚构没有提供的审查细节，本记录不推断验收人姓名、工号、设备版本、PDF/XLSX 软件版本、13 项逐项结果或签名，而采用可追溯的结果性声明：

| 字段 | 记录值 |
|---|---|
| 决策来源 | `project_owner_current_session` |
| 受审固定候选 | `53b8ff3e0d6768543bd7a3c8cc0295f5dc3e7b98` |
| 已验证 Windows ZIP | `E:\code\alert_management_system\.runtime\release\artifacts\53b8ff3e0d6768543bd7a3c8cc0295f5dc3e7b98\alert-management-system-windows-x64-53b8ff3e0d67.zip` |
| ZIP SHA-256 | `ea09168a5ea318dd0680c58ec5867ed8b7693f2fcb884a87b445b71a259fc9c0` |
| 声明记录时间 | `2026-08-27T18:01:06+08:00` |
| 结论 | `PASS` |
| 正式发布授权 | `v1.0.0` 已授权；仍以发布 PR 和远端 CI 成功为技术前提 |

项目负责人确认后，只允许说明书、项目报告、专利草案、发布治理、版本元数据和对应生成物变化。业务源码、数据库迁移、算法实现、样例或用户可见流程变化会使该声明对新候选失效。

## 当前边界

自动证据证明固定候选可构建、安装、部署、处理新输入、拒绝错误输入、解释结果、展示数据库事实、生成报告、备份恢复并安全停止。它不证明长期 7×24 稳定性、任意负载、现场模型指标或法规认证。项目负责人已关闭交付可行性门槛并授权正式发布；`v1.0.0` 仍只在本轮允许路径静态检查、发布 PR 和远端 CI 成功后创建。

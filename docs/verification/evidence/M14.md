# M14 Windows 11 业务用户终验与发布候选证据

- 阶段/门槛：M14 / G14
- 固定实现候选：`2dd3d484a5ed03e51ffdc56f612e0855027fa470`
- 候选分支：`feat/m14-business-release`
- 自动复核日期：2026-08-27
- 目标平台：Windows 11 x64
- 发布版本：`1.0.0-rc.1`
- 自动门槛判定：PASS
- AC-022 非技术业务人员终验：PENDING；不得由智能体代填或代签

## 固定候选与产物

Windows 自包含候选：

```text
E:\code\alert_management_system\.runtime\release\artifacts\2dd3d484a5ed03e51ffdc56f612e0855027fa470\alert-management-system-windows-x64-2dd3d484a5ed.zip
```

SHA-256：

```text
6fe819c7fa11a8b98c24bd7d4517ccd60277927f2c23dae7cf3d30912c1278e0
```

同名 `.sha256` 文件、ZIP 内 `manuals` 目录、`release-manifest.json` 与自动摘要均由正式入口生成。哈希文件与重新计算结果一致；发布摘要的 `source_commit` 精确等于固定实现候选，`status=PASS`、`standard_user_precheck=PASS`、`human_business_acceptance=REQUIRED`。

## 自动验收

以下三条固定命令均在干净工作树的固定候选上成功执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/release/verify-business-release.ps1
```

Windows 标准用户实际完成：构建自包含 ZIP、损坏 ZIP 哈希拒绝、ASCII 与中文空格双目录、首次登录与改密、项目和三格式样例、分析与解释、人工修订和处置、PDF/XLSX 报告、备份状态与容量、外部恢复点保留、跨实例隔离恢复、20,000 行闭环、停止、计划任务/PID/端口/Junction 清理。技术摘要位于：

```text
E:\code\alert_management_system\.runtime\release\verification\20260827-065306-806d191f\verification-summary.json
```

```bash
python3 tests/smoke/run.py --target docker --fresh-volume
```

结果为 PASS，耗时 `734.723` 秒。本机回环与 HTTPS 网络模式分别从空项目卷启动，完成健康失败注入、CSV/TXT/XLSX 导入、固定算法分析、处置审计和摘要一致性，并确认明文及内部端口边界和精确资源清理。摘要位于：

```text
.runtime/m7/20260827-070032-584189-98cbd232/verification-summary.json
```

```bash
python3 scripts/validate_release_candidate.py --mode candidate --expected-commit 2dd3d484a5ed03e51ffdc56f612e0855027fa470
```

结果为 PASS。校验覆盖 M0–M13 检查点、57 项来源责任、交付物确定性、工作树、版本入口、本地/远端标签边界和人工记录为空。AC-022 之后只有状态、证据、来源闭环和正式项目报告生成物可以变化；源码、脚本、配置、样例、工作流、业务手册、部署手册或打包输入变化都会使人工结果失效并要求重建候选。

定向检查同时通过：发布校验器 8 项单测、PowerShell 5.1 AST 解析、仓库基础检查、自动化工作流检查、八份 DOCX/PDF 确定性检查及 `git diff --check`。

## 远端固定提交检查

以下运行的 `headSha` 均精确为 `2dd3d484a5ed03e51ffdc56f612e0855027fa470`，且均为 SUCCESS：

- 仓库基础检查：run `33020839365`。
- M1 应用全链：run `33020839241`。
- M12 恢复质量与可靠性：run `33020839279`。
- M6 Windows 原生发布：run `33020839267`。

Docker 工作流未由本次仅涉及原生清理和状态文件的路径过滤触发；同一固定提交已在目标 Windows 11 主机执行工作流中的固定 Docker 命令并生成上述 PASS 摘要，未以旧提交结果替代。

## 独立复审与缺陷闭环

三个只读复审者分别检查业务/UI、Windows 输出与清理边界、发布生命周期与标签边界。最终结论均为 PASS，无 BLOCKER、MAJOR 或 MINOR：

- 业务/UI：确认产品源码、样例、两份手册及严格 401 夹具未被审计修正改变；0、2、3 个恢复点的页面与后端语义一致；业务/部署手册不在 AC-022 后允许变更白名单中。
- Windows 清理：确认输出根、所有既存路径段、marker、summary 和递归删除目标拒绝 ReparsePoint；Junction 负例只作非递归删除且外部哨兵保持不变；PostgreSQL PID、进程、命令行和 Junction 身份在任何 schedule/stop 之前缓存，删除前再次验证目标。
- 生命周期：定向拒绝源码、原生脚本、Compose、样例、手册、生成手册、包内 README 和工作流变化；四种生命周期模式均权威拒绝远端 `v1.0.0` 提前存在，候选/批准模式同时拒绝 RC 标签复用。

较早候选的页面计数、重复清理、FileInfo 路径、重解析点和人工验收后变更边界问题已按失败证据作最小修正；它们不作为本候选通过依据。

## AC-022 人工业务终验（待填写）

本节当前只固定待验对象，不表示人工 PASS。验收人必须未参与候选开发，只取得上述 ZIP、同名 `.sha256` 和 ZIP 内两份手册，在 Windows 11 x64 上不阅读源码、不依赖口头补充，逐项填写真实结果：

| 固定步骤键 | 当前状态 |
| --- | --- |
| `hash_and_extract` | 待人工 |
| `preflight` | 待人工 |
| `start_and_open_ui` | 待人工 |
| `first_login_and_password_change` | 待人工 |
| `project_and_sample_import` | 待人工 |
| `analysis_and_explanation` | 待人工 |
| `revision_assignment_and_disposition` | 待人工 |
| `pdf_and_xlsx_reports` | 待人工 |
| `backup_status_and_creation` | 待人工 |
| `isolated_restore` | 待人工 |
| `stop_and_data_retention` | 待人工 |
| `manual_without_oral_supplement` | 待人工 |
| `chinese_display_and_system_stability` | 待人工 |

还必须填写 Windows 版本、浏览器、PDF 阅读器、XLSX 阅读器、验收人、业务角色、签名、开始/签署时间、阻断/严重缺陷数和最终结论。只有 13 项均为 PASS、阻断和严重缺陷均为 0，且原始记录与 `automation/state.json` 的结构化字段一致，阶段才能从 `awaiting_human` 恢复到 `review`。人工 PASS 不自动授权 `dev → main` 合并，也不自动创建 `v1.0.0-rc.1` 或 `v1.0.0`。

## 当前边界

自动证据证明固定候选可以构建、部署、运行、处理新输入、解释分析、生成报告、备份恢复并安全停止；不证明未执行的真实现场准确率、法规认证、任意负载、长期 7×24 稳定性或公司的后续专用模型效果。当前唯一未完成门槛是 AC-022 人工可理解性与独立操作终验。

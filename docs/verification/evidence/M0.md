# M0 事实冻结复核证据

- 阶段/门槛：M0 / G0
- 被测提交：`6d10fc175044a53f9c3d457e23f93962bdee2f95`
- 被测分支检查点：`origin/dev`
- 复核日期：2026-08-25
- 平台：Windows 11 x64 上的 WSL2，Node.js 22.22.1
- 判定：通过

## 执行记录

先在指向被测提交的独立临时 worktree 中运行检查。第一次直接执行提取复核时，因为干净 worktree 尚未安装锁定依赖，Node.js 返回 `ERR_MODULE_NOT_FOUND`（缺少 `@napi-rs/canvas`）；该失败属于缺少准备步骤，未被计为通过。

随后仍在新的独立临时 worktree 中执行：

```text
npm --prefix tools/document-extraction ci
python3 scripts/validate_repository.py
npm --prefix tools/document-extraction run check
```

三条命令退出码均为 0：锁文件安装 34 个包且审计无漏洞；仓库基础检查通过；37 个提取结果与受控来源逐字节一致。提取工具输出一条 PDF 字体函数警告，但未改变文件一致性判定。

复核后执行 `git ls-remote origin refs/heads/dev`，远端 `dev` 当时解析到同一被测提交。自动化工作流及其验证器是在 M0 检查点之后建立，因此不倒填为该提交的验收命令。

## 证据范围

本记录只证明 M0 的来源隔离、产品事实、架构、计划及提取可重复性门槛。它不证明 M1 业务骨架已经实现，也不沿用历史材料中的结项、准确率或性能陈述。

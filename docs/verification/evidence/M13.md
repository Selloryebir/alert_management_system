# M13 正式说明书与项目过程文件证据

- 阶段/门槛：M13 / G13
- 固定实现候选：`0b0f63094065a40580c6d97ee853c245612664f1`
- 基线：`46bf2bac2f5cc6827b9eef8f941e600d46f3857c`（`origin/dev`）
- 候选分支：`feat/m13-formal-deliverables`（已合并并清理）
- 复核日期：2026-08-27
- 目标平台：Windows 11 x64
- 独立对抗复审：两路 PASS；一路仅提出自然语言守卫的非阻断增强建议
- 最终判定：PASS；固定候选经 PR 合入 `dev`，合并树一致，且合并检查点的本机 G13 与五条远端检查全部通过

## 实现范围

M13 没有引入第二套产品事实或手工维护的二进制文档：

- `README.md`、业务使用说明和 Windows 部署运维说明覆盖先决条件、安装、启动、登录、样例闭环、人工补录、项目生命周期、数据库备份恢复、停止和故障处理。
- 立项、中期、测试、结项、开发过程和原始方案差距说明以当前产品契约、代码和固定提交证据为准，不继承历史材料中未经验证的人员、预算、试点、准确率、合规或长期稳定性声明。
- 八份 Markdown 是唯一事实源；生成器确定性地产生八份 DOCX、八份 PDF 和清单，并检查中文字体、页数、来源摘要与生成文件摘要。
- Windows 原生 ZIP 包含业务使用说明和部署说明的 DOCX/PDF，发布清单对文件和发布版本进行校验。
- GitHub 仓库检查在 Linux 与 Windows 的 Python 3.12 环境安装带哈希的二进制依赖，并验证生成物没有漂移。
- 前端项目删除入口与后端授权一致，仅系统管理员可见；项目负责人仍可归档和恢复项目。

## 固定候选验收

以下命令均在干净工作树的 `0b0f63094065a40580c6d97ee853c245612664f1` 上执行：

- `python3 -m unittest discover -s tools/deliverables/tests`：PASS，5 项。
- `python3 tools/deliverables/build.py --check`：PASS，8 份 DOCX、8 份 PDF 与清单均和事实源一致。
- `python3 scripts/validate_repository.py`：PASS。
- `python3 scripts/validate_automation.py`：PASS。
- `python3 scripts/validate_formal_baseline.py`：PASS。
- `npm --prefix src/frontend test -- --run tests/M11.test.ts --maxWorkers=1`：PASS，8 项。
- 仓库工作流 YAML 解析、Windows PowerShell 构建/校验脚本解析和 `git diff --check`：PASS。

Windows 原生完整验收命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/native/verify-release.ps1
```

结果为 PASS。自包含 ZIP 为 `.runtime/native/artifacts/0b0f63094065a40580c6d97ee853c245612664f1/alert-management-system-windows-x64-0b0f63094065.zip`，SHA-256 为 `024f04ba2bc53da920f9d8dc3947cfa157248eee99f7e298f0b9f94bf4714993`；证据目录为 `.runtime/native/verification/20260827-023342-eaa7213a`。同一包在 ASCII 路径和中文空格路径中均完成预检、启动、浏览器业务闭环、报告、备份、隔离恢复、负控、停止和清理。

## 远端固定提交检查

以下五条运行的 `headSha` 均精确为 `0b0f63094065a40580c6d97ee853c245612664f1`，且均为 SUCCESS：

- 仓库基础检查：run `32999936534`。
- M1 应用全链：run `32999936602`。
- M12 恢复质量与可靠性：run `32999936596`。
- M6 Windows 原生发布：run `32999936652`。
- M7 Docker Compose：run `32999936539`。

## 独立复审与缺陷闭环

三个只读复审者分别检查业务入口与角色、前后端事实映射、文档声明与交付链。两路最终结论为 PASS；前后端权限、人工补录字段、项目生命周期、部署目录、备份外置、清单版本和交付物摘要均与实现一致。

一路复审指出：仅依靠词法规则不可能证明任意中文句子的限定语义。该项不阻断本阶段，因为八份受控事实源已逐份审查且未包含被禁止声明，生成守卫的职责是阻止可预见的高风险回归，而不是充当中文语义证明器。继续枚举所有自然语言变体不会提高当前可达交付的实质可靠性；若后续新增正式材料，仍须同时经过生成检查和人工事实审阅。

两个较早候选保留为失败证据，不作为通过依据：

1. `922108cf`：远端 YAML 解析失败；部署路径、备份外置、业务说明、发布版本和声明守卫存在事实差距。
2. `ec9563b0`：冒号可借用限定词；项目负责人页面展示了后端仅系统管理员可执行的删除入口。

上述问题均在固定候选中以最小修改关闭，并有定向测试或文档生成检查覆盖。

## PR 与 dev 集成检查点

PR [#13](https://github.com/Selloryebir/alert_management_system/pull/13) 已从 `feat/m13-formal-deliverables` 合入 `dev`。受审分支头为 `20ba22894f1f290c26f0c19a14765f90d5c4b04b`，非快进合并检查点为 `4be7ea550f250001e58e403d72ab086ca4e85d90`。两者的树 SHA-1 均为 `636ca7a21d3c23c2d01b8636307890bf02004f63`，证明合并未引入未受审内容。PR 合并前为 CLEAN/MERGEABLE，无评论、无阻断审查，七项实际检查全部成功。

在 `dev` 合并检查点上重新执行三条 M13 正式验收命令均为 PASS。文档生成命令使用仓库忽略目录中的隔离 Python 3.12 环境和带哈希锁文件；系统 Python 缺少依赖时曾明确失败，没有被记为通过。

以下五条 push 运行的 `headSha` 均精确为 `4be7ea550f250001e58e403d72ab086ca4e85d90`，且均为 SUCCESS：

- 仓库基础检查：run `33002333995`。
- M1 应用全链：run `33002334014`。
- M12 恢复质量与可靠性：run `33002333947`。
- M6 Windows 原生发布：run `33002333976`。
- M7 Docker Compose：run `33002333990`。

因此 `origin/dev` 可解析的 `4be7ea5` 成为 M13 阶段检查点，并解锁 M14 发布候选阶段。

## 证据边界

本证据证明固定候选的正式说明、过程文件和生成链与当前代码及有限验收事实一致，且文档可随 Windows 原生包交付。它不把合成数据结果表述为真实现场准确率，不承诺未经执行的试点、7×24 小时稳定性、任意硬件性能、法律合规认证或公司模型效果。

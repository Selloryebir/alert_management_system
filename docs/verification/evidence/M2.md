# M2 合成数据与导入闭环复核证据

- 阶段/门槛：M2 / G2
- 固定候选提交：`ffeb7495c517f1f5bcf1c550159049606d4ad6c7`
- 候选分支：`origin/feat/m2-import-pipeline`
- 复核日期：2026-08-25
- 本机平台：Windows 11 x64、WSL2、Windows JDK 21.0.8、WSL Node.js 22.22.1、Python 3.12.14、Docker Desktop、PostgreSQL 17.6
- 远端平台：GitHub Actions Ubuntu runner、Java 21、Python 3.12、Node.js 22、PostgreSQL 17.6 容器
- 独立审查：PASS
- 判定：候选门槛通过，等待合入 `dev` 后记录集成检查点

## 实现范围

候选实现固定种子的多格式合成样例、CSV/制表符 TXT/XLSX 解析、UTF-8/GB18030 编码识别、显式字段映射、全文件校验、两阶段原子导入、批次列表/详情/分页原始行追溯 API，以及 Vue 最小导入向导。没有提前实现 M3 分析或 M4 看板、处置流程。

## 本机作者检查

执行：

```text
scripts/dev/test.sh
scripts/dev/m2-import-smoke.sh
git diff --check
```

结果：后端 21 项、Python 算法 9 项、合成数据 5 项、Vue 交互 5 项测试通过；前端生产构建通过。PostgreSQL 17.6 冒烟结果：

- CSV、制表符 TXT、XLSX 各 300 行，数据库全字段规范化摘要均为 `f51aaa1a28a57440bfd42853434e7316`。
- GB18030 中文 CSV 12 行导入成功。
- 六个非法文件精确命中预期清单：1 个文件级错误、36 个逐行 `source_row/field/code` 错误；六批 `valid_rows=0`，`import_staging` 与 `alarm_record` 均为 0。
- 批次列表可查询；300 行批次通过两页遍历，首尾 `source_row=2/301`，原始载荷可追溯。
- 重复确认返回 HTTP 409 和稳定中文错误契约，原批次记录数不变。
- 固定 seed `20260825` 的 20,000 行、4,852,439 bytes Demo 完成导入：作者检查预览 2346 ms、确认 1325 ms、总计 3671 ms。

嵌入式 PostgreSQL 14.22 集成测试另覆盖显式字段映射、Flyway V1/V2、严格枚举、缺表头/映射失败归零、列表与分页参数边界、不存在批次、强制确认失败整体回滚和重复确认。

## 独立审查记录

第一次固定候选 `3099b9e7edb125e4d13d4559f558dbdc61aa8dd3` 的独立审查为 FAIL：发现部分被声明为非法的枚举/时间值实际可被接受，缺表头仍报告有效行，且 M2 计划中的列表、分页追溯和前端最小导入向导缺失。没有降低门槛；修复后新增精确预期清单、对应自动断言及缺失闭环。

独立审查者仅针对最终固定候选 `ffeb7495c517f1f5bcf1c550159049606d4ad6c7` 复核，结论为 PASS，无未关闭阻断或严重问题。审查者重新执行两条正式门槛；其 PostgreSQL 17.6 的 20,000 行结果为预览 2255 ms、确认 1653 ms、总计 3908 ms，并确认工作树干净、生成目录未被提交、差异未越过 M2。

## 远端执行记录

- 最终 SHA 仓库基础检查：成功，run `32835799441`。
- 应用检查：成功，run `32835727952`；完整执行组件测试、两轮重启和 M2 导入冒烟。该 run 的代码提交为 `a88367d39618fa1a4b164c2f535032c49725d15e`，与最终固定候选仅根 README 三处状态文字不同。
- 远端 PostgreSQL 17.6 的 20,000 行结果：预览 1659 ms、确认 421 ms、总计 2080 ms。

GitHub Actions 对部分官方 action 给出 Node 20 兼容层和 `setup-java@v4` 弃用提示，不影响当前命令退出码或 M2 结果；该提示不在本阶段做无关升级。

## 证据边界

本记录只证明 M2 合成样例、文件导入、校验、原始行追溯及最小导入向导。它不证明报警分析准确率、看板与处置、报告导出、Windows 自包含发布包或 Docker Compose 交付已经完成。

## `dev` 集成检查点

本节在候选非快进合入 `dev` 并完成远端检查后填写；在此之前 M3 保持锁定。

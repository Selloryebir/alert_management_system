# M1 可运行工程骨架复核证据

- 阶段/门槛：M1 / G1
- 固定候选提交：`bc1887a8c084f5b5da7b2ec63076715ba2b0ce2e`
- 候选分支：`origin/feat/m1-runnable-foundation`
- 复核日期：2026-08-25
- 本机平台：Windows 11 x64、WSL2、Windows JDK 21.0.8、WSL Node.js 22.22.1、Python 3.12.14、Docker Desktop
- 远端平台：GitHub Actions Ubuntu runner、Java 21、Python 3.12、Node.js 22、PostgreSQL 17.6 容器
- 独立审查：PASS
- 判定：候选门槛通过，等待合入 `dev` 后记录集成检查点

## 实现范围

候选仅建立 M1 所需的可运行四组件骨架：Java/Spring Boot 主系统、Python/FastAPI 算法健康服务、Vue 状态页和 PostgreSQL/Flyway V1。页面明确标识“2026 年灾后重建 Demo”和“仅使用合成数据”；没有提前实现 M2 导入或 M3 分析端点。

## 本机执行记录

在同一候选内容上执行：

```text
scripts/dev/test.sh
scripts/dev/restart-smoke.sh
bash -n scripts/dev/*.sh
PowerShell AST 解析 scripts/dev/start-processes.ps1
git diff --check
```

结果：仓库与自动化校验通过；Java 12 项、Python 9 项、前端 3 项测试通过；两轮启停均得到 `system/database/algorithm=UP`；Vue 静态资源身份检查通过；第二轮日志确认 Flyway V1 已是最新、未重复迁移；Shell、PowerShell 和差异检查通过。

另执行以下可达失败检查：

- 完全停机后 `scripts/dev/status.sh` 返回非零，并报告进程停止、数据库退出及健康地址 DOWN。
- 单独停止算法服务后，Java 顶层健康为 `DEGRADED`；`status.sh` 精确报告后端 DOWN 并返回非零，没有被嵌套组件的 UP 误导。
- 在服务已运行时连续执行两次 `scripts/dev/start.sh`，两次均成功，未再触发 Windows JAR 文件锁。

## 独立审查记录

独立审查者仅针对固定候选 `bc1887a8c084f5b5da7b2ec63076715ba2b0ce2e` 复核，结论为 PASS，无未关闭阻断或严重问题。复核确认：

- 算法健康探针严格校验 `status/service/version/contract_version`，并覆盖错误身份、版本、契约、缺字段、非 2xx、非法 JSON 和连接失败。
- Linux/Windows 停止逻辑同时核验 PID 对应命令身份；PowerShell 部分启动失败会回收已启动进程。
- `mvnw` 和全部 `scripts/dev/*.sh` 在 Git 提交树中为 `100755`。
- 工作树差异未越过 M1，也未伪造后续业务闭环。

## 远端执行记录

- 仓库基础检查：成功，run `32829892698`。
- M1 应用骨架检查：成功，run `32829892826`，耗时约 1 分 28 秒；完整执行组件测试、两轮集成冒烟和停止清理。

首个候选 `6c069bab88c4fb47d6e87711e1e7705f20500e55` 的应用 CI 在测试前因 Shell 文件模式为 `100644` 返回 126。没有放宽检查；新候选仅补齐 `mvnw` 与开发脚本的 `100755` 模式并重新通过全部 CI。GitHub Actions 对部分官方 action 的 Node 20 兼容层给出弃用提示，不影响本次命令退出码或 M1 运行结果，留待相关 action 版本的独立维护增量处理。

## 证据边界

本记录只证明 M1 工程骨架、健康契约、迁移、状态页与开发启停闭环。它不证明数据导入、报警分析、报告导出、Windows 自包含发布包或 Docker Compose 次级交付已经完成。

合入 `dev` 后应在本节追加远端可达的非快进合并提交，并以该提交作为 M1 `checkpoint_commit`，之后才允许解锁 M2。

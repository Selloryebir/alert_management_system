# 合成数据验证

运行：

```bash
python3 -m pytest tests/data -q
```

测试仅使用 Python 标准库读取 CSV、TXT 和 XLSX 内容，并调用合成数据生成器；pytest 是仓库测试运行器，不是生成器运行依赖。测试固定校验格式等价、首个可见 XLSX 工作表、编码样例、无效场景行数、摘要哈希以及 20,000 行 Demo 的可重复生成。

摘要常量独立于 Java 导入实现。生成器或契约有意变更时，必须人工审阅样例差异后再更新常量，不得由待测 Java 代码生成预期结果。
